# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Native application

The active application is at the repository root. `sveltemo/` is the previous PWA and is reference material only.

Before changing Convex code, read `convex/_generated/ai/guidelines.md`.

Use `npm run typecheck`, `npm run lint`, and `npm test` for validation. Use `npm run ios:build` to build and open the native iOS application.

Before publishing a native build, OTA update, or backend change that can affect installed clients, use `.agents/skills/release-review/SKILL.md`. Follow `docs/releases.md`. Never raise minimum supported versions as an automatic part of a build or deployment.
